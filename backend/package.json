{
  "name": "eemmic-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the EEMMIC website — receives the contact form and newsletter signup. Database storage is not yet wired up (see backend/src/db.js).",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
