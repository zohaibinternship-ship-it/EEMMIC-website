# Copy this file to ".env" and fill in real values. Never commit ".env".

# TODO: add the new database's connection variables here once it's chosen.

# Port the backend server listens on (local dev only — Vercel ignores this).
PORT=3000
