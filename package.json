{
  "name": "eemmic-site",
  "version": "1.0.0",
  "private": true,
  "description": "EEMMIC website — root manifest so Vercel installs backend dependencies for api/index.js.",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
